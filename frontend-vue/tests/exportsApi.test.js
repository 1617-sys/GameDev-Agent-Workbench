import test from "node:test";import assert from "node:assert/strict";
test("export API binds version, idempotency and retry to project scope",async()=>{global.window={setTimeout,clearTimeout,sessionStorage:{getItem:()=>"jwt"}};const calls=[];global.fetch=async(url,options={})=>{calls.push([url,options]);return{ok:true,status:200,json:async()=>({code:0,data:{jobUuid:"job",status:"COMPLETED"}})}};const{exportsApi}=await import("../src/shared/api/exports.js");await exportsApi.create("project","version","key");await exportsApi.retry("project","job");assert.match(calls[0][0],/projects\/project\/prototype-versions\/version\/exports$/);assert.equal(calls[0][1].headers["Idempotency-Key"],"key");assert.match(calls[1][0],/projects\/project\/exports\/job\/retry$/);});
