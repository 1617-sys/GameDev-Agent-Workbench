FROM node:22-alpine
WORKDIR /app

RUN addgroup -S app && adduser -S -G app app
COPY --chown=app:app src ./src

ENV SIMULATION_SERVICE_PORT=8090
EXPOSE 8090
USER app
CMD ["node", "src/features/demo/runtime/simulation-service/server.ts"]
