## 1. API 事实源与权限底座

- [x] 1.1 导出 prod 与 non-prod OpenAPI 快照并建立包含全部现有 Controller 方法的覆盖元数据，运行覆盖脚本验证无重复、无未分类端点
- [x] 1.2 为覆盖元数据定义 `audience`、`lifecycle`、`frontendFeature`、`dangerLevel`、`owner` 和 profile schema，添加合法值与必填字段测试
- [x] 1.3 增加 OpenAPI、覆盖元数据和前端 API 映射的漂移检查，添加一个未分类测试端点并验证 CI 测试会失败
- [x] 1.4 在后端建立稳定的用户 capability 声明并扩展认证响应或只读能力接口，使用普通用户和管理员授权测试验证不可由客户端提权
- [x] 1.5 在前端 session store 中消费 capability keys 并实现可复用路由守卫，使用直接访问管理员 URL 的组件/路由测试验证 403 状态

## 2. 角色分层前端外壳

- [x] 2.1 重构主导航为普通用户、项目高级和管理员/诊断分组，使用不同 capability fixtures 验证可见项完全符合授权
- [x] 2.2 新增加载、空数据、权限不足、网络失败、部分缺失和危险确认共享组件，使用组件测试验证安全文本和操作禁用状态
- [x] 2.3 为新增页面建立懒加载路由和统一页面布局，运行前端生产构建并验证桌面与 375px 视口无横向溢出

## 3. GameSpec 创作语义与 V5 桥接

- [x] 3.1 先添加失败测试证明当前默认 GameSpec 会让“从零生成”发送非空 `currentSpec`，再实现两个互斥创作模式并验证从零模式发送 `null`
- [x] 3.2 为修改模式添加合法 JSON 门禁和当前规格摘要，使用非法 JSON、合法修改和模式切换测试验证请求语义
- [x] 3.3 设计并实现 GenerationRun 到 PrototypeVersion 的幂等来源绑定与兼容性诊断，使用成功、重复提交、跨项目和不兼容契约测试验证
- [x] 3.4 在生成台增加“创建/查看原型版本”入口并显示 V4/V5 兼容状态，使用页面测试验证不兼容时按钮禁用且原因可见
- [x] 3.5 接入已有 PrototypeVersion 创建 API 或新的桥接 API，使用真实请求契约测试验证幂等键、来源摘要和导航目标

## 4. Player Run 与 Episode 完整操作

- [x] 4.1 为 Player Run 创建接口补充前端 API 适配器和请求 DTO 校验测试，验证版本、Persona、预算、幂等键和错误映射
- [x] 4.2 在版本页增加 Player Run 启动表单与成本/预算确认，使用成功、重复点击、无权限和后端失败组件测试验证
- [x] 4.3 将完整 Player Run 详情接口接入 Episode 页面，验证运行中、失败、无 Episode 和完成状态均忠实展示
- [x] 4.4 接入完整 Machine Episode 详情并保留分页步骤与聚合视图，使用长轨迹和空批次测试验证分页边界

## 5. Artifact 总览

- [x] 5.1 为项目 Artifact 列表补充 API 适配器、筛选和分页模型，使用编码项目 UUID、空列表和错误响应契约测试验证
- [x] 5.2 新增 Artifact 总览与详情页并从运行、生成和版本页面提供双向入口，使用跨项目 UUID 越权 E2E 验证内容不泄露

## 6. 知识库与 RAG 证据

- [x] 6.1 建立 knowledge API/Store，接入文档上传和列表接口，使用合法文件、超限文件、恶意文件名、403 和状态变化测试验证
- [x] 6.2 新增项目知识库页面和安全状态轮询，使用 READY、FAILED、空库和移动端组件测试验证
- [x] 6.3 建立 RAG evidence API/Store 并接入 WorkflowRun 详情，使用 RAG-on、RAG-off、无候选、检索失败和 mock fixtures 验证状态区分
- [x] 6.4 添加文档与引用内容的转义和字段白名单测试，验证脚本、HTML、Prompt 注入文本和长文件名不会执行或溢出

## 7. Prompt 运维与指标

- [x] 7.1 审查 PromptTemplate 现有查询/修改/更新接口的可发现性，必要时补充最小分页列表接口并用管理员、普通用户和跨用户测试验证授权
- [x] 7.2 建立 prompt-ops API/Store 和模板管理页面，使用读取、差异确认、更新失败、审计结果与敏感字段 fixtures 验证
- [x] 7.3 建立 PromptVersion analytics API/Store 和筛选页面，使用时间窗口、项目、Agent 类型、includeMock、零样本和缺失 usage 测试验证
- [x] 7.4 验证指标数值完全来自后端聚合且前端不重新计算 P95、成本或成功率，添加契约测试锁定展示字段和单位

## 8. Dashboard、Agent Run 与诊断

- [x] 8.1 接入项目汇总和 Agent 类型汇总接口并新增管理员 Dashboard，使用空数据、部分缺失、超长名称和 375px 布局测试验证
- [x] 8.2 接入 Agent Run 创建、列表和详情接口到受控管理员工作区，使用筛选、分页、成本确认、幂等和敏感字段测试验证
- [x] 8.3 新增诊断页面展示健康状态、运行 profile、内部/废弃接口与替代路径，验证 prod 环境不出现非生产 Demo 执行按钮
- [x] 8.4 为旧版 Workflow、非生产 Demo 和内部 Machine Episode 写入接口确定最终 lifecycle 分类，更新覆盖矩阵并验证每项都有所有者与替代/保留理由

## 9. 集成与发布门禁

- [x] 9.1 为普通用户、项目高级用户和管理员分别编写主导航到目标页面的 Playwright E2E，验证跨角色 URL 和 API 越权均被拒绝
- [x] 9.2 运行前端全部单元测试、生产构建、V4/V5 浏览器 E2E 和相关 Java 测试，并保存命令、版本、通过数与失败证据
- [x] 9.3 对 Cocos Creator 3.8.8 的 GameSpec→构建→预览→审批→发布链执行回归，验证新增导航和权限不改变现有状态机
- [x] 9.4 将端点覆盖门禁切换为严格模式，验证所有后端端点均为已映射、internal 或 deprecated 且不存在 planned/unknown 项
- [ ] 9.5 更新用户文档、角色权限矩阵、端点覆盖矩阵和运维回滚说明，并由人工逐页验收普通用户与管理员界面
  - 文档、权限矩阵、覆盖矩阵和回滚说明已完成；保留未勾选，等待项目验收人员使用真实普通用户与管理员账号逐页签字。
