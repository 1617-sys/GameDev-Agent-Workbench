global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: gamedev-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend-java:8081"]
        labels:
          environment: local-compose
